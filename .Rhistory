full_data <- merge(results_80, byd_rating, by = "FID")
cor(full_data[, c("mean", "sd", "skewness", "kurtosis", "byd")], use = "complete.obs")
library(corrplot)
corrplot(cor(full_data[, c("mean", "sd", "skewness", "kurtosis", "byd")], use = "complete.obs"),
method = "number", type = "upper")
plot(full_data$skewness, full_data$byd)
model <- lm(byd ~ mean + skewness + kurtosis, data = full_data)
summary(model)
hist(values(DEM), breaks = 100, main = "Distribution of Plant Height Growth",
xlab = "Height (m)", col = "lightgreen")
summary(values(DEM))
quantile(values(DEM), probs = seq(0, 1, 0.1), na.rm = TRUE)
results_df
# Count NA and NaN values
sum(is.na(values(ht_plot)))       # Count of NA or NaN values
ht_plot
exists("ht_plot")
print'ht_plot'
print(ht_plot)
print("ht_plot")
#load libraries
library(sf)
library(terra)
library(ggplot2)
#load DEM raster
dem0 <- rast("/carl-data/shared/users/sanshre/DPON_2024/Stillwater/ODM_DSM/2024_01_03_ST_M_odm_dsm_modified.tif")
dem1 <- rast("/carl-data/shared/users/sanshre/DPON_2024/Stillwater/ODM_DSM/2024_04_29_ST_M_odm_dsm.tif")
dem0_resampled <- resample(dem0, dem1)
DEM <- dem1 - dem0_resampled
shapfil <- st_read("/carl-data/shared/carl/gcp_detail/shapefile_2024_stw/shape_grid_ST_02_20_2024.shp")
shapfil <- st_transform(shapfil, crs = crs(DEM, proj = TRUE))
#load orthophoto and calculate ndvi
orthophoto <- rast("/carl-data/shared/users/sanshre/DPON_2024/Stillwater/DPON-2024-04-29/M_combined_newV/odm_orthophoto/odm_orthophoto.tif")
nir <- orthophoto[[4]]
red <- orthophoto[[1]]
ndvi <- (nir-red) / (nir+red+0.000001)
# Create empty list to store results
results <- list()
# Loop through each FID
for (fid in shapfil$FID) {
# Get the polygon geometry for the current FID
plot_geom <- shapfil[shapfil$FID == fid, ]
# Crop/mask NDVI to the plot
vi_plot <- crop(ndvi, plot_geom)
vi_plot <- mask(vi_plot, plot_geom)
# Crop/mask and resample height to the same plot
ht_plot <- crop(DEM, plot_geom)
ht_plot <- mask(ht_plot, plot_geom)
ht_plot <- resample(ht_plot, vi_plot)
# Calculate height threshold based on average plot plant height
height_threshold <- mean(values(ht_plot), na.rm = TRUE) * 0.80
# Apply subsetting to only pixels with height above threshold
vi_sub <- values(vi_plot)[values(ht_plot) > height_threshold]
# Compute plot-level statistics
vi_stats <- list(
FID = fid,
mean = mean(vi_sub, na.rm = TRUE),
sd = sd(vi_sub, na.rm = TRUE),
skewness = e1071::skewness(vi_sub, na.rm = TRUE),
kurtosis = e1071::kurtosis(vi_sub, na.rm = TRUE)
)
# Store results
results[[as.character(fid)]] <- vi_stats
}
results_df <- do.call(rbind, lapply(results, as.data.frame))
results_df <- data.frame(FID = names(results), results_df)
write.csv(results_df, "ndvi_stats_by_plot.csv", row.names = FALSE)
# Count missing values
sum(is.na(values(ht_plot)))
# Count pixels with height = 0
sum(values(ht_plot) == 0, na.rm = TRUE)
class(ht_plot)     # Should return: "SpatRaster"
is.null(ht_plot)   # Should be FALSE
# Count missing values
sum(is.na(values(ht_plot)))
# Count pixels with height = 0
sum(values(ht_plot) == 0, na.rm = TRUE)
# Count number of NA values
sum(is.na(vi_sub))
ncell(vi_plot)
dim(vi_plot)
163*36
vi_sub
plot(vi_sub)
ht_thresh <- mean(values(ht_plot), na,rm = TRUE) * 0.20
#load libraries
library(sf)
library(terra)
library(ggplot2)
#load DEM raster
dem0 <- rast("/carl-data/shared/users/sanshre/DPON_2024/Stillwater/ODM_DSM/2024_01_03_ST_M_odm_dsm_modified.tif")
dem1 <- rast("/carl-data/shared/users/sanshre/DPON_2024/Stillwater/ODM_DSM/2024_04_29_ST_M_odm_dsm.tif")
dem0_resampled <- resample(dem0, dem1)
DEM <- dem1 - dem0_resampled
shapfil <- st_read("/carl-data/shared/carl/gcp_detail/shapefile_2024_stw/shape_grid_ST_02_20_2024.shp")
shapfil <- st_transform(shapfil, crs = crs(DEM, proj = TRUE))
#load orthophoto and calculate ndvi
orthophoto <- rast("/carl-data/shared/users/sanshre/DPON_2024/Stillwater/DPON-2024-04-29/M_combined_newV/odm_orthophoto/odm_orthophoto.tif")
nir <- orthophoto[[4]]
red <- orthophoto[[1]]
ndvi <- (nir-red) / (nir+red+0.000001)
# Create empty list to store results
results <- list()
# Loop through each FID
for (fid in shapfil$FID) {
# Get the polygon geometry for the current FID
plot_geom <- shapfil[shapfil$FID == fid, ]
# Crop/mask NDVI to the plot
vi_plot <- crop(ndvi, plot_geom)
vi_plot <- mask(vi_plot, plot_geom)
# Crop/mask and resample height to the same plot
ht_plot <- crop(DEM, plot_geom)
ht_plot <- mask(ht_plot, plot_geom)
ht_plot <- resample(ht_plot, vi_plot)
# Calculate height threshold based on average plot plant height
#height_threshold <- mean(values(ht_plot), na.rm = TRUE) * 0.80
ht_thresh <- mean(values(ht_plot), na,rm = TRUE) * 0.20
# Apply subsetting to only pixels with height above threshold
vi_sub <- values(vi_plot)[values(ht_plot) > height_threshold]
# Compute plot-level statistics
vi_stats <- list(
FID = fid,
mean = mean(vi_sub, na.rm = TRUE),
sd = sd(vi_sub, na.rm = TRUE),
skewness = e1071::skewness(vi_sub, na.rm = TRUE),
kurtosis = e1071::kurtosis(vi_sub, na.rm = TRUE)
)
# Store results
results[[as.character(fid)]] <- vi_stats
}
# Create empty list to store results
results <- list()
# Loop through each FID
for (fid in shapfil$FID) {
# Get the polygon geometry for the current FID
plot_geom <- shapfil[shapfil$FID == fid, ]
# Crop/mask NDVI to the plot
vi_plot <- crop(ndvi, plot_geom)
vi_plot <- mask(vi_plot, plot_geom)
# Crop/mask and resample height to the same plot
ht_plot <- crop(DEM, plot_geom)
ht_plot <- mask(ht_plot, plot_geom)
ht_plot <- resample(ht_plot, vi_plot)
# Calculate height threshold based on average plot plant height
#height_threshold <- mean(values(ht_plot), na.rm = TRUE) * 0.80
height_threshold <- mean(values(ht_plot), na,rm = TRUE) * 0.20
# Apply subsetting to only pixels with height above threshold
vi_sub <- values(vi_plot)[values(ht_plot) > height_threshold]
# Compute plot-level statistics
vi_stats <- list(
FID = fid,
mean = mean(vi_sub, na.rm = TRUE),
sd = sd(vi_sub, na.rm = TRUE),
skewness = e1071::skewness(vi_sub, na.rm = TRUE),
kurtosis = e1071::kurtosis(vi_sub, na.rm = TRUE)
)
# Store results
results[[as.character(fid)]] <- vi_stats
}
# Loop through each FID
for (fid in shapfil$FID) {
# Get the polygon geometry for the current FID
plot_geom <- shapfil[shapfil$FID == fid, ]
# Crop/mask NDVI to the plot
vi_plot <- crop(ndvi, plot_geom)
vi_plot <- mask(vi_plot, plot_geom)
# Crop/mask and resample height to the same plot
ht_plot <- crop(DEM, plot_geom)
ht_plot <- mask(ht_plot, plot_geom)
ht_plot <- resample(ht_plot, vi_plot)
# Calculate height threshold based on average plot plant height
#height_threshold <- mean(values(ht_plot), na.rm = TRUE) * 0.80
height_threshold <- mean(values(ht_plot), na.rm = TRUE) * 0.20
# Apply subsetting to only pixels with height above threshold
vi_sub <- values(vi_plot)[values(ht_plot) > height_threshold]
# Compute plot-level statistics
vi_stats <- list(
FID = fid,
mean = mean(vi_sub, na.rm = TRUE),
sd = sd(vi_sub, na.rm = TRUE),
skewness = e1071::skewness(vi_sub, na.rm = TRUE),
kurtosis = e1071::kurtosis(vi_sub, na.rm = TRUE)
)
# Store results
results[[as.character(fid)]] <- vi_stats
}
vi_sub
plot(vi_sub)
# Create empty list to store results
results <- list()
# Loop through each FID
for (fid in shapfil$FID) {
# Get the polygon geometry for the current FID
plot_geom <- shapfil[shapfil$FID == fid, ]
# Crop/mask NDVI to the plot
vi_plot <- crop(ndvi, plot_geom)
vi_plot <- mask(vi_plot, plot_geom)
# Crop/mask and resample height to the same plot
ht_plot <- crop(DEM, plot_geom)
ht_plot <- mask(ht_plot, plot_geom)
ht_plot <- resample(ht_plot, vi_plot)
# Calculate height threshold based on average plot plant height
#height_threshold <- mean(values(ht_plot), na.rm = TRUE) * 0.80
height_threshold <- mean(values(ht_plot), na.rm = TRUE) * 0.02
# Apply subsetting to only pixels with height above threshold
vi_sub <- values(vi_plot)[values(ht_plot) > height_threshold]
# Compute plot-level statistics
vi_stats <- list(
FID = fid,
mean = mean(vi_sub, na.rm = TRUE),
sd = sd(vi_sub, na.rm = TRUE),
skewness = e1071::skewness(vi_sub, na.rm = TRUE),
kurtosis = e1071::kurtosis(vi_sub, na.rm = TRUE)
)
# Store results
results[[as.character(fid)]] <- vi_stats
}
vi_sub
height_threshold
# Calculate height threshold based on average plot plant height
#height_threshold <- mean(values(ht_plot), na.rm = TRUE) * 0.80
height_threshold <- mean(values(ht_plot), na.rm = TRUE) * 0.8
height_threshold
ht_plot
# Calculate height threshold based on average plot plant height
#height_threshold <- mean(values(ht_plot), na.rm = TRUE) * 0.80
height_threshold <- mean(values(ht_plot), na.rm = TRUE)
height_threshold
ht_plot
ht_plot
height_threshold
# Apply subsetting to only pixels with height above threshold
vi_sub <- values(vi_plot)[values(ht_plot) > height_threshold]
vi_sub
mean(v_sub)
mean(vi_sub)
# Compute plot-level statistics
vi_stats <- list(
FID = fid,
mean = mean(vi_sub, na.rm = TRUE),
sd = sd(vi_sub, na.rm = TRUE),
skewness = e1071::skewness(vi_sub, na.rm = TRUE),
kurtosis = e1071::kurtosis(vi_sub, na.rm = TRUE)
)
mean(vi_sub)
mean(vi_sub, na.rm = TRUE)
# Calculate height threshold based on average plot plant height
height_threshold <- mean(values(ht_plot), na.rm = TRUE) * 0.80
height_threshold
# Apply subsetting to only pixels with height above threshold
vi_sub <- values(vi_plot)[values(ht_plot) > height_threshold]
mean(vi_sub, na.rm = TRUE)
# Calculate height threshold based on average plot plant height
height_threshold <- mean(values(ht_plot), na.rm = TRUE) * 0.50
height_threshold
# Apply subsetting to only pixels with height above threshold
vi_sub <- values(vi_plot)[values(ht_plot) > height_threshold]
mean(vi_sub, na.rm = TRUE)
# Calculate height threshold based on average plot plant height
height_threshold <- mean(values(ht_plot), na.rm = TRUE) * 0.90
ht_plot
# Calculate height threshold based on average plot plant height
height_threshold <- mean(values(ht_plot), na.rm = TRUE) * 0.90
# Crop/mask and resample height to the same plot
ht_plot <- crop(DEM, plot_geom)
# Get the polygon geometry for the current FID
plot_geom <- shapfil[shapfil$FID == fid, ]
# Crop/mask NDVI to the plot
vi_plot <- crop(ndvi, plot_geom)
vi_plot <- mask(vi_plot, plot_geom)
ndvi <- (nir-red) / (nir+red+0.000001)
#load libraries
library(sf)
library(terra)
library(ggplot2)
#load DEM raster
dem0 <- rast("/carl-data/shared/users/sanshre/DPON_2024/Stillwater/ODM_DSM/2024_01_03_ST_M_odm_dsm_modified.tif")
dem1 <- rast("/carl-data/shared/users/sanshre/DPON_2024/Stillwater/ODM_DSM/2024_04_29_ST_M_odm_dsm.tif")
dem0_resampled <- resample(dem0, dem1)
DEM <- dem1 - dem0_resampled
shapfil <- st_read("/carl-data/shared/carl/gcp_detail/shapefile_2024_stw/shape_grid_ST_02_20_2024.shp")
shapfil <- st_transform(shapfil, crs = crs(DEM, proj = TRUE))
#load orthophoto and calculate ndvi
orthophoto <- rast("/carl-data/shared/users/sanshre/DPON_2024/Stillwater/DPON-2024-04-29/M_combined_newV/odm_orthophoto/odm_orthophoto.tif")
nir <- orthophoto[[4]]
red <- orthophoto[[1]]
ndvi <- (nir-red) / (nir+red+0.000001)
quarto preview Draft_PP.qmd --to pdf
quarto preview Draft_PP.qmd --to pdf
sudo apt install texlive-full
quarto render Proposal_RE.qmd --to pdf --log-output
#load libraries
library(sf)
library(terra)
library(ggplot2)
#load DEM raster
dem0 <- rast("/carl-data/shared/users/sanshre/DPON_2024/Stillwater/ODM_DSM/2024_01_03_ST_M_odm_dsm_modified.tif")
dem1 <- rast("/carl-data/shared/users/sanshre/DPON_2024/Stillwater/ODM_DSM/2024_04_29_ST_M_odm_dsm.tif")
dem0_resampled <- resample(dem0, dem1)
DEM <- dem1 - dem0_resampled
shapfil <- st_read("/carl-data/shared/carl/gcp_detail/shapefile_2024_stw/shape_grid_ST_02_20_2024.shp")
shapfil <- st_transform(shapfil, crs = crs(DEM, proj = TRUE))
#load orthophoto and calculate ndvi
orthophoto <- rast("/carl-data/shared/users/sanshre/DPON_2024/Stillwater/DPON-2024-04-29/M_combined_newV/odm_orthophoto/odm_orthophoto.tif")
nir <- orthophoto[[4]]
red <- orthophoto[[1]]
ndvi <- (nir-red) / (nir+red+0.000001)
# Create empty list to store results
results <- list()
# Loop through each FID
for (fid in shapfil$FID) {
# Get the polygon geometry for the current FID
plot_geom <- shapfil[shapfil$FID == fid, ]
# Crop/mask NDVI to the plot
vi_plot <- crop(ndvi, plot_geom)
vi_plot <- mask(vi_plot, plot_geom)
# Crop/mask and resample height to the same plot
ht_plot <- crop(DEM, plot_geom)
ht_plot <- mask(ht_plot, plot_geom)
ht_plot <- resample(ht_plot, vi_plot)
ht_plot
# Calculate height threshold based on average plot plant height
height_threshold <- mean(values(ht_plot), na.rm = TRUE) * 0.80
height_threshold <- mean(values(ht_plot), na.rm = TRUE)
height_threshold
# Apply subsetting to only pixels with height above threshold
vi_sub <- values(vi_plot)[values(ht_plot) > height_threshold]
mean(vi_sub, na.rm = TRUE)
# Compute plot-level statistics
vi_stats <- list(
FID = fid,
mean = mean(vi_sub, na.rm = TRUE),
sd = sd(vi_sub, na.rm = TRUE),
skewness = e1071::skewness(vi_sub, na.rm = TRUE),
kurtosis = e1071::kurtosis(vi_sub, na.rm = TRUE)
)
# Store results
results[[as.character(fid)]] <- vi_stats
}
# Get the polygon geometry for the current FID
plot_geom <- shapfil[shapfil$FID == fid, ]
ht_plot
# Calculate height threshold based on average plot plant height
height_threshold <- mean(values(ht_plot), na.rm = TRUE) * 0.80
height_threshold
# Apply subsetting to only pixels with height above threshold
vi_sub <- values(vi_plot)[values(ht_plot) > height_threshold]
mean(vi_sub, na.rm = TRUE)
height_threshold <- mean(values(ht_plot), na.rm = TRUE)
height_threshold
# Apply subsetting to only pixels with height above threshold
vi_sub <- values(vi_plot)[values(ht_plot) > height_threshold]
mean(vi_sub, na.rm = TRUE)
height_threshold <- mean(values(ht_plot), na.rm = TRUE) * 0.90
height_threshold
# Apply subsetting to only pixels with height above threshold
vi_sub <- values(vi_plot)[values(ht_plot) > height_threshold]
mean(vi_sub, na.rm = TRUE)
height_threshold <- mean(values(ht_plot), na.rm = TRUE) * 0.70
height_threshold
# Apply subsetting to only pixels with height above threshold
vi_sub <- values(vi_plot)[values(ht_plot) > height_threshold]
mean(vi_sub, na.rm = TRUE)
height_threshold <- mean(values(ht_plot), na.rm = TRUE) * 0.60
height_threshold
# Apply subsetting to only pixels with height above threshold
vi_sub <- values(vi_plot)[values(ht_plot) > height_threshold]
mean(vi_sub, na.rm = TRUE)
height_threshold <- mean(values(ht_plot), na.rm = TRUE) * 0.50
height_threshold
# Apply subsetting to only pixels with height above threshold
vi_sub <- values(vi_plot)[values(ht_plot) > height_threshold]
mean(vi_sub, na.rm = TRUE)
height_threshold <- mean(values(ht_plot), na.rm = TRUE) * 0.20
height_threshold
# Apply subsetting to only pixels with height above threshold
vi_sub <- values(vi_plot)[values(ht_plot) > height_threshold]
mean(vi_sub, na.rm = TRUE)
plot_geom <- shapfil[shapfil$FID == 533, ]
# Crop/mask NDVI to the plot
vi_plot <- crop(ndvi, plot_geom)
vi_plot <- mask(vi_plot, plot_geom)
# Crop/mask and resample height to the same plot
ht_plot <- crop(DEM, plot_geom)
ht_plot <- mask(ht_plot, plot_geom)
ht_plot <- resample(ht_plot, vi_plot)
ht_plot
# Calculate height threshold based on average plot plant height
height_threshold <- mean(values(ht_plot), na.rm = TRUE) * 0.80
height_threshold
vi_sub <- values(vi_plot)[values(ht_plot) > height_threshold]
mean(vi_sub, na.rm = TRUE)
height_threshold <- mean(values(ht_plot), na.rm = TRUE) * 0.20
height_threshold
vi_sub <- values(vi_plot)[values(ht_plot) > height_threshold]
> mean(vi_sub, na.rm = TRUE)
vi_sub <- values(vi_plot)[values(ht_plot) > height_threshold]mean(vi_sub, na.rm = TRUE)
vi_sub <- values(vi_plot)[values(ht_plot) > height_threshold]
mean(vi_sub, na.rm = TRUE)
height_threshold <- mean(values(ht_plot), na.rm = TRUE) * 0.50
height_threshold
vi_sub <- values(vi_plot)[values(ht_plot) > height_threshold]
vi_sub
mean(vi_sub, na.rm = TRUE)
height_threshold <- mean(values(ht_plot), na.rm = TRUE) * 0.90
height_threshold
vi_sub <- values(vi_plot)[values(ht_plot) > height_threshold]
mean(vi_sub, na.rm = TRUE)
height_threshold <- mean(values(ht_plot), na.rm = TRUE) * 1
> height_threshold
height_threshold
vi_sub <- values(vi_plot)[values(ht_plot) > height_threshold]
mean(vi_sub, na.rm = TRUE)
plot_geom <- shapfil[shapfil$FID == 535, ]
> # Crop/mask NDVI to the plot
vi_plot <- crop(ndvi, plot_geom)
vi_plot <- mask(vi_plot, plot_geom)
ht_plot <- crop(DEM, plot_geom)
ht_plot <- mask(ht_plot, plot_geom)
ht_plot <- resample(ht_plot, vi_plot)
ht_plot
height_threshold <- mean(values(ht_plot), na.rm = TRUE) * 0.80
height_threshold
vi_sub <- values(vi_plot)[values(ht_plot) > height_threshold]
mean(vi_sub, na.rm = TRUE)
height_threshold <- mean(values(ht_plot), na.rm = TRUE) * 0.90
> height_threshold
height_threshold
vi_sub <- values(vi_plot)[values(ht_plot) > height_threshold]
> mean(vi_sub, na.rm = TRUE)
mean(vi_sub, na.rm = TRUE)
height_threshold <- mean(values(ht_plot), na.rm = TRUE) * 0.50> height_threshold
vi_sub <- values(vi_plot)[values(ht_plot) > height_threshold]
mean(vi_sub, na.rm = TRUE)
height_threshold <- mean(values(ht_plot), na.rm = TRUE) * 1> height_threshold
> vi_sub <- values(vi_plot)[values(ht_plot) > height_threshold]
vi_sub <- values(vi_plot)[values(ht_plot) > height_threshold]
mean(vi_sub, na.rm = TRUE)
plot_geom <- shapfil[shapfil$FID == 1535, ]
vi_plot <- crop(ndvi, plot_geom)
vi_plot <- mask(vi_plot, plot_geom)
ht_plot <- crop(DEM, plot_geom)
ht_plot <- mask(ht_plot, plot_geom)
ht_plot <- resample(ht_plot, vi_plot)
height_threshold <- mean(values(ht_plot), na.rm = TRUE) * 0.80
height_threshold
vi_sub <- values(vi_plot)[values(ht_plot) > height_threshold]
mean(vi_sub, na.rm = TRUE)
height_threshold <- mean(values(ht_plot), na.rm = TRUE) * 0.50
height_threshold
vi_sub <- values(vi_plot)[values(ht_plot) > height_threshold]
mean(vi_sub, na.rm = TRUE)
height_threshold <- mean(values(ht_plot), na.rm = TRUE) * 0.20
height_threshold
vi_sub <- values(vi_plot)[values(ht_plot) > height_threshold]
mean(vi_sub, na.rm = TRUE)
height_threshold <- mean(values(ht_plot), na.rm = TRUE) * 1
height_threshold
vi_sub <- values(vi_plot)[values(ht_plot) > height_threshold]
mean(vi_sub, na.rm = TRUE)
plot_geom <- shapfil[shapfil$FID == 2418, ]
vi_plot <- crop(ndvi, plot_geom)
plot_geom <- shapfil[shapfil$FID == 2415, ]
> vi_plot <- crop(ndvi, plot_geom)
vi_plot <- crop(ndvi, plot_geom)
vi_plot <- mask(vi_plot, plot_geom)
ht_plot <- crop(DEM, plot_geom)
ht_plot <- mask(ht_plot, plot_geom)
ht_plot <- resample(ht_plot, vi_plot)
height_threshold <- mean(values(ht_plot), na.rm = TRUE) * 1
height_threshold
vi_sub <- values(vi_plot)[values(ht_plot) > height_threshold]
mean(vi_sub, na.rm = TRUE)
height_threshold <- mean(values(ht_plot), na.rm = TRUE) * 0.90
height_threshold
vi_sub <- values(vi_plot)[values(ht_plot) > height_threshold]
mean(vi_sub, na.rm = TRUE)
height_threshold <- mean(values(ht_plot), na.rm = TRUE) * 0.80
height_threshold
vi_sub <- values(vi_plot)[values(ht_plot) > height_threshold]mean(vi_sub, na.rm = TRUE)
vi_sub <- values(vi_plot)[values(ht_plot) > height_threshold]
mean(vi_sub, na.rm = TRUE)
height_threshold <- mean(values(ht_plot), na.rm = TRUE) * 0.95
height_threshold
vi_sub <- values(vi_plot)[values(ht_plot) > height_threshold]
mean(vi_sub, na.rm = TRUE)
height_threshold <- mean(values(ht_plot), na.rm = TRUE) * 0.96
> height_threshold
height_threshold <- mean(values(ht_plot), na.rm = TRUE) * 0.96
height_threshold
vi_sub <- values(vi_plot)[values(ht_plot) > height_threshold]
mean(vi_sub, na.rm = TRUE)
height_threshold <- mean(values(ht_plot), na.rm = TRUE) * 0.97
height_threshold
vi_sub <- values(vi_plot)[values(ht_plot) > height_threshold]
mean(vi_sub, na.rm = TRUE)
height_threshold <- mean(values(ht_plot), na.rm = TRUE) * 0.98
height_threshold
vi_sub <- values(vi_plot)[values(ht_plot) > height_threshold]
mean(vi_sub, na.rm = TRUE)
height_threshold <- mean(values(ht_plot), na.rm = TRUE) * 0.98
height_threshold
vi_sub <- values(vi_plot)[values(ht_plot) > height_threshold]
mean(vi_sub, na.rm = TRUE)
height_threshold <- mean(values(ht_plot), na.rm = TRUE) * 0.99
height_threshold
vi_sub <- values(vi_plot)[values(ht_plot) > height_threshold]
mean(vi_sub, na.rm = TRUE)
height_threshold <- mean(values(ht_plot), na.rm = TRUE) * 1
height_threshold
vi_sub <- values(vi_plot)[values(ht_plot) > height_threshold]
mean(vi_sub, na.rm = TRUE)
